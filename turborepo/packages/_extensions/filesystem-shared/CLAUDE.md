# @repo/extension-filesystem-shared

Client-safe helpers for admin filesystem views (formatters, lightweight utilities). Import these from UI components instead of the Node-focused `@repo/extension-filesystem-utils` package.
