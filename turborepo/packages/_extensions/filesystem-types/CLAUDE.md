# @repo/extension-filesystem-types

Shared TypeScript contracts for admin-facing filesystem tooling. Use these types everywhere (Node utilities, routers, React hooks) to guarantee shape stability across packages.
