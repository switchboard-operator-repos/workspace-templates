# @repo/trpc-hooks-utils

Tiny helper utilities shared by all TRPC hook packages. Provides the `RouterProxy` type alias and runtime guards (`ensureProxyTarget`) so hook factories can fail fast when routers are missing.
